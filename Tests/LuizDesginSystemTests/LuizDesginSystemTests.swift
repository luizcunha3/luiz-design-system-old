import XCTest
@testable import LuizDesginSystem

final class LuizDesginSystemTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(LuizDesginSystem().text, "Hello, World!")
    }
}
